#include <stdio.h>
#include <string.h>
#include <algorithm>

using namespace std;

typedef long long ll;

#define N 1000000000

int main() {
    int T; scanf("%d", &T);
    while (T--) {
        ll n, k;
        scanf("%lld%lld", &n, &k);
        int res
    }
    return 0;
}
