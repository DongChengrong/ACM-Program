#include<stdio.h>
#include<string.h>
int A[40][40];
int r,l,len;
void dfs(int w,int h,int *s,int *e)
{
}
int main()
{
    int t,w,h;
    int *s,*e;
    int i;
    scanf("%d",&t);
    while(t--)
    {
        scanf("%d%d",&w,&h);
        for(i=0;i<h;i++)
        {
            gets(A[i]);
        }
        s=strchr(A,S);
        e=strchr(A,E);
    }
    return 0;
}
