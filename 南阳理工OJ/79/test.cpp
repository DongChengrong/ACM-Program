#include<stdio.h>
#include<string.h>
#include<algorithm>
using namespace std;

const int maxn = 1000 + 20;
char s1[maxn],s2[maxn];
int d[maxn][maxn];

int main()
{
    int T;
    scanf("%d",&T);
   // while(T--)
    //{
        memset(d,1,sizeof(d));
        printf("%d\n",d[0][0]);
   // }
    return 0;
}
